# Copy to .env and fill in real values. .env itself is gitignored — never commit real keys.

# Used by agent/cost_logger.py and all later triage/resolve agent phases.
ANTHROPIC_API_KEY=sk-ant-...

# Only needed once Phase 4 (Slack notifications) is wired up.
SLACK_WEBHOOK_URL=
